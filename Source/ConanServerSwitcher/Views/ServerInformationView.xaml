<Window x:Class="ConanServerSwitcher.Views.ServerInformationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
		DataContext="{Binding ServerInformationViewModel, Source={StaticResource DependencyInjector}}"
        Title="Server Configuration" 
        Height="300" Width="350" 
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<StackPanel HorizontalAlignment="Stretch" Margin="10">
			<Label Content="_Server Name" />
			<TextBox Margin="0,0,0,4" Text="{Binding ServerName}" />

			<Label Content="Server _Address" />
			<TextBox Margin="0,0,0,4" Text="{Binding ServerAddress}"/>

			<Label Content="Server _Port" />
			<TextBox Margin="0,0,0,4" Text="{Binding ServerPort}" />

			<Label Content="_Mod list file" />
			<TextBox Margin="0,0,0,4"  Text="{Binding ModListPath}"/>

			<Button Content="Browse for file" Command="{Binding BrowseForFile}" />
		</StackPanel>

		<Grid Grid.Row="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="120" />
				<ColumnDefinition Width="120" />
			</Grid.ColumnDefinitions>

			<Button Grid.Column="1" Content="_OK" Margin="5" Height="25" Command="{Binding DialogAccept}" />
			
			<Button Grid.Column="2" Content="_Cancel" Margin="5" Command="{Binding DialogCancel}" />
		</Grid>
	</Grid>
</Window>
